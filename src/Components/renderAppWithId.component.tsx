import { FunctionComponent } from 'react';
import AppDetails from './appDetails.component';

const RenderAppWithId: FunctionComponent = (props) => {
  return <AppDetails />;
};

export default RenderAppWithId;
