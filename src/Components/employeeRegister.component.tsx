import { FunctionComponent } from 'react';

//Employee needs only to provide company email and password to register. Other information is seeded into db at hiring.
const EmployeeRegister: FunctionComponent = () => {
  return <h3>Seed db with other info, only password </h3>;
};

export default EmployeeRegister;
