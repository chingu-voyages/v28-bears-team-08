import { FunctionComponent } from 'react';

export interface AppDetailsProps {}

const AppDetails: FunctionComponent<AppDetailsProps> = () => {
  return <h3>app details to make each row of the list</h3>;
};

export default AppDetails;
